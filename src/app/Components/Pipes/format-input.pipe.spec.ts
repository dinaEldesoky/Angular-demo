import { FormatInputPipe } from './format-input.pipe';

describe('FormatInputPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatInputPipe();
    expect(pipe).toBeTruthy();
  });
});
