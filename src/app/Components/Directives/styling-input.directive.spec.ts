import { StylingInputDirective } from './styling-input.directive';

describe('StylingInputDirective', () => {
  it('should create an instance', () => {
    const directive = new StylingInputDirective();
    expect(directive).toBeTruthy();
  });
});
